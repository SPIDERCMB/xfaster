#!/usr/bin/env python

import xfaster as xf

# parse arguments
args = xf.xfaster_parse()
mode = args.pop("mode")

if mode == "submit":
    # submit a job
    xf.xfaster_submit(**args)

elif mode == "run":
    # run the analysis
    xf.xfaster_run(**args)
